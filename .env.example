ANTHROPIC_API_KEY=sk-replace-me

# Optional: override the model defined in prompts/v1.yaml at runtime.
CNI_MODEL=

# Which versioned prompt file to load from prompts/ (v1, v2, …).
CNI_PROMPT_VERSION=v1

# Paths (defaults are fine if you run from the repo root).
CNI_PROMPTS_DIR=prompts
