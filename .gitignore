# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/
env/

# Secrets — never commit your API key
.env
*.key

# OS / tooling
.DS_Store

# Regenerated by the POC / notebook on each run
data/notes/scanned_note*.png

# Per-run audit artifacts (each run writes its own; keep the sample committed one)
runs/*
!runs/sample_run/

# NOTE: deliverables/video/presentation.mp4 IS committed on purpose (submission requires it).
